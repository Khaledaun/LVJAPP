# Claude.md Re-baseline Prompt — v3.2 → v4.0

**Audience:** A fresh Claude Code session, read cold.
**Scope:** Re-baseline `Claude.md` from v3.2 to v4.0 to align with `docs/PRD.md` v0.3 and decisions `D-007` through `D-019`.
**Save this file at:** `docs/prompts/CLAUDE_MD_REBASELINE_v4.md`

> **Numbering note.** The original draft of this prompt referenced
> `D-006 … D-020`. On merge the decision block was reconciled against
> the existing `docs/DECISIONS.md` (which already used `D-006`
> "drafting owns intent" and `D-007` "design source"). The existing
> `D-006` / `D-007` were renumbered to `D-020` / `D-021`, and the PRD
> v0.3 block landed at its canonical `D-007` … `D-019`. The body of this
> prompt is preserved verbatim below; where a specific `D-NNN` is cited
> it refers to the canonical numbering in `docs/DECISIONS.md`.

-----

## Paste-ready prompt

> You are re-baselining `Claude.md` for the LVJ AssistantApp project. The PRD has been revised (v0.3) and 14 decisions (`D-006` through `D-020`) have been ratified. `Claude.md` v3.2 is now out of alignment. Your job: bring `Claude.md` into alignment without breaking landed code.
>
> **Read order, in this exact sequence:**
>
> 1. `docs/PRD.md` (v0.3 — the new product contract; *source of truth where it conflicts with `Claude.md`*).
> 1. `docs/DECISIONS.md` (read `D-006` through `D-020` carefully — these are the binding values you must encode).
> 1. `Claude.md` (v3.2 — the file you will edit).
> 1. `docs/AGENT_OS.md` (so you understand what the AOS contract assumes).
> 1. `docs/EXECUTION_LOG.md` (so you know what has actually landed and you don't re-engineer it).
>
> **Your deliverable is a single PR that:**
>
> 1. Updates `Claude.md` end-to-end as specified below.
> 1. Bumps the version header to **v4.0** and updates the date.
> 1. Appends an entry to `docs/EXECUTION_LOG.md` describing the changes.
> 1. Adds new skill files where called out below (just the SKILL.md scaffolds).
> 1. **Does not touch any source code.** This PR is documentation-only. Code changes follow in subsequent PRs (Sprint 0.1 onwards per D-020).
>
> **Hard rules while editing:**
>
> - Preserve all 8 Golden Rules verbatim.
> - Preserve the LVJ Authority Design System tokens unchanged (extend with Arabic faces per D-015 — see Phase 2 instructions below).
> - Preserve the Documentation Discipline section unchanged.
> - Do not delete any sprint history from the Execution Status snapshot — only add to it.
> - Anywhere you remove US-immigration content, leave a one-line note pointing to `D-006`.
>
> *(Remainder of the prompt body — Section-by-section changes, New skill
> files to scaffold, Documentation discipline tasks, Acceptance criteria
> — is the same body delivered by the founder on 2026-04-22. It is the
> source that drove this PR; see the PR description for the executed
> summary.)*

-----

## Reconciliation notes applied during execution

- **`D-006` / `D-007` collision.** Existing `D-006` ("Drafting owns
  intent") and `D-007` ("Design source = claude.ai/design pack") were
  renumbered to `D-020` and `D-021` respectively. The PRD v0.3 decision
  block landed at `D-007` … `D-019` as written. `D-003` ("Arabic /
  multilingual UI on hold") is marked `superseded-by: D-015`.
- **User-prompt D-NNN offsets.** The prompt above refers to, e.g.,
  "Stripe Connect Day-1 (D-017)" and "Public Provider Directory
  (D-016)". The canonical numbering in `docs/DECISIONS.md` is
  `D-016` = Stripe Connect and `D-017` = Public Provider Directory.
  Same pattern for D-011 (cost caps in the prompt → D-012 canonical),
  D-012 (HITL tiers in the prompt → D-013 canonical), D-013 (availability
  in the prompt → D-014 canonical), D-018 (onboarding in the prompt → no
  standalone decision; lives inside `D-019` sprint priority), D-019
  (analytics in the prompt → D-018 canonical), D-020 (sprint priority in
  the prompt → D-019 canonical). Where `Claude.md` v4.0 cites a
  decision, it uses the canonical number.
- **`docs/PRD.md`.** The file previously existed as `docs/Pdr` (no
  extension). Renamed in this PR.

-----

*End of prompt record. Do not edit this file after landing — it is a
traceability artefact.*
